HashMap is a class
Map interface;
Key-Value pair;
Duplicate: not for key, yes for value;
To add an entry: hm.put(k,v);
Two objects (k,v) are required for adding;





HashSet is a class
Set interface;
Key only; 
Duplicates: not for key, N/A for value, there is no value in HashSet;
To add an entry: hs.add(obj);
One object (obj) is required for adding;

